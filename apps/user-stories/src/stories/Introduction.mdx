# Componentry

A modern component library built with Stencil and distributed for multiple frameworks.

## Features

* Framework agnostic
* React support
* Vue support
* Angular support
* Native Web Components
* Light & Dark themes
* Design tokens
* Accessible by default
* Type-safe APIs

## Installation

### React

```bash
npm install @componentry-ui/react
```

```tsx
import { CButton } from '@componentry-ui/react'

export function App() {
  return <CButton>Click Me</CButton>
}
```

### Vue

```bash
npm install @componentry-ui/vue
```

```vue
<template>
  <c-button>Click Me</c-button>
</template>
```

### Angular

```bash
npm install @componentry-ui/angular
```

```ts
import { CButton } from '@componentry-ui/angular'
```

```html
<c-button>Click Me</c-button>
```

### Web Components

```bash
npm install @componentry-ui/stencil
```

```ts
import { defineCustomElements } from '@componentry-ui/stencil/loader'

defineCustomElements()
```

```html
<c-button>Click Me</c-button>
```

## Packages

| Package | Description |
|----------|-------------|
| `@componentry-ui/react` | React components |
| `@componentry-ui/vue` | Vue components |
| `@componentry-ui/angular` | Angular components |
| `@componentry-ui/stencil-core` | Web Components |
| `@componentry-ui/theme` | Design tokens and themes |

## Theming

Componentry includes built-in light and dark themes.

```html
<html class="c-theme-light">
```

```html
<html class="c-theme-dark">
```
